angular
    .module('buttonApp', ['firebase']);


    // 2 - add 'firebase' as a dependency here! What this means is that our module depends on firebase in order to run properly. We simply add the names of any other modules we need into the array.
    //meaning add 'firebase inside empty array'